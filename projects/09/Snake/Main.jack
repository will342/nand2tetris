/** Initializes a new snake game and starts running it. */

class Main {
    function void main() {
        var SnakeGame game;
        let game = SnakeGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}

/**
class Main {
    function void main() {
        var SnakeTest test;
        let test = SnakeTest.new();
        do test.run();
        //do game.dispose();
        return;
    }
}
*/
